upstream django_app {
    server web:8000;
}

server {
    listen 80;
    # REPLACE WITH YOUR ACTUAL DOMAIN
    server_name dimavoyage.com; 

    # Allow Certbot to verify your domain
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all HTTP to HTTPS (Security)
    # TEMPORARILY COMMENTED OUT UNTIL SSL IS READY
    # location / {
    #     return 301 https://$host$request_uri;
    # }
}

# --- COMMENT OUT THIS WHOLE BLOCK FOR NOW ---
# server {
#     listen 443 ssl;
#     server_name dimavoyage.com;
#
#     ssl_certificate /etc/letsencrypt/live/dimavoyage.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dimavoyage.com/privkey.pem;
#
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location /static/ {
#         alias /code/staticfiles/;
#     }
#
#     location /media/ {
#         alias /code/media/;
#     }
#
#     location / {
#         proxy_pass http://django_app;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Host $host;
#         proxy_redirect off;
#     }
# }
